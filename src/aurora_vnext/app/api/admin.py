"""
Aurora OSI vNext — Admin API (STUB)
Implemented in Phase O

Endpoints (when implemented) — ALL require admin role:
  GET   /api/v1/admin/audit
  GET   /api/v1/admin/users
  POST  /api/v1/admin/users
  PATCH /api/v1/admin/users/{id}
  GET   /api/v1/admin/config
  PATCH /api/v1/admin/config             — writes audit record before executing
  GET   /api/v1/admin/version-registry
  POST  /api/v1/admin/version-registry   — writes audit record before executing
"""

# Phase O implementation placeholder