"""
Aurora OSI vNext — Commodity Registry (STUB)
Implemented in Phase F

Commodity lookup, family resolution, and version tracking.
  get_commodity(name) → CommodityDefinition
  get_family(family_id) → FamilyDefinition
  resolve_family(commodity, province) → FamilyDefinition
  get_commodity_library_version() → str
"""

# Phase F implementation placeholder